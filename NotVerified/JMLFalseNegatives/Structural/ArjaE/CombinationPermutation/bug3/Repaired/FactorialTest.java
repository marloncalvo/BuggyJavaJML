
import static org.junit.Assert.*;
import org.junit.Test;
public class FactorialTest {
	@Test
	void test() {
		int n = 0;
		assertEquals(new Factorial().factorial(n), 1);
	}

	@Test
	void test1() {
		int n = 0;
		assertEquals(new Factorial().factorial(n), 1);
	}
}
