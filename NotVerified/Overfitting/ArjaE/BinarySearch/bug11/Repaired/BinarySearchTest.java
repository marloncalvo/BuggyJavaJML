import static org.junit.Assert.*;

import org.junit.Test;

public class BinarySearchTest {

    /**
     * Counterexample array is too large. This might be false negative
     * since low + 1 will only change low>high's result iff low == high,
     * but we if low==high, we will still want to return -1.
     */
	@Test
	public void test() {
		fail("The function Binary() is not testable for this case");
	}

}
